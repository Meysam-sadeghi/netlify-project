{
  "name": "netlify-app",
  "version": "1.3.0",
  "description": "Netlify Edge app",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  }
}
